[package]
name = "nif_extractous"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "nif_extractous"
crate-type = ["cdylib"]

[dependencies]
extractous = "0.3.0"
rustler = { version = "0.36", default-features = false, features = ["derive", "nif_version_2_17"] }